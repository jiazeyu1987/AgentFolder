# Agent Dashboard Backend (Windows)

## Setup
- Install deps: `D:\miniconda3\python.exe -m pip install -r dashboard_backend/requirements.txt`
- Run: `powershell -ExecutionPolicy Bypass -File run_backend.ps1`

API base: `http://127.0.0.1:8000`
